#include <iostream>
using namespace std;
// el objetivo del programa sera realizar una cuenta regresiva desde 10 hasta 0
int main() {
    // se utiliza un ciclo for para la cuenta regresiva
    for (int i = 10; i >= 0; --i)  {
        cout << i << endl;
    }

    return 0;
}
